import React from 'react';
import useToolkit from '../toolkit/useToolkit';

const RefHtmlparser2 = ({}) => {
  const { RefLibrary } = useToolkit();
  return (
    <RefLibrary
      url="https://github.com/fb55/htmlparser2#readme"
      name="htmlparser2"
    />
  );
};

export default RefHtmlparser2;
